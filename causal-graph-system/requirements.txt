# 核心框架
python>=3.9
pyyaml>=6.0
networkx>=3.0
tqdm>=4.65

# 数据结构与校验（可选，schema 用 dataclass 实现，pydantic 非必需）
# pydantic>=2.5

# 文本处理 / 事件抽取（模块1，按需启用）
transformers>=4.36
torch>=2.1
# spacy>=3.7
jieba>=0.42
# hanlp>=2.1

# 关系分类（模块2，按需启用）
# scikit-learn>=1.3
# openai>=1.0        # 如用 LLM 做少样本/生成式因果抽取

# 图算法 / 推理（模块3，按需启用）
# scipy>=1.11

# 测试
pytest>=7.4
