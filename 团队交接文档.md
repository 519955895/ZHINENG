# 因果关系抽取模块 — 团队交接文档

> 模块 2：因果关系抽取（`src/relation/`）
> 日期：2026-09-23
> 作者：何振鹏

---

## 一、项目概述

本模块从事件列表中抽取因果关系对，输出 `CausalRelation`（cause_event_id → effect_event_id + 置信度 + 关系类型）。支持两种模式：

| 模式 | 方法 | 适用场景 |
|------|------|---------|
| 规则模式 | 57 个因果提示词 + 特征加权 | 无 GPU、快速原型 |
| ML 模式 | BERT / sklearn 分类器打分 | 高召回率、语义因果 |

---

## 二、需要交接的文件清单

### 2.1 核心代码（`src/relation/`）

| 文件 | 用途 | 对接方关注点 |
|------|------|------------|
| [causal_relation.py](file:///d:/何振鹏/可视化/ZHINENG/causal-graph-system/src/relation/causal_relation.py) | **主入口** `extract_relations(events, config, ml_scorer)` | 调用方只需关心这个函数 |
| [ml_scorer.py](file:///d:/何振鹏/可视化/ZHINENG/causal-graph-system/src/relation/ml_scorer.py) | ML 打分器（BERT/sklearn），含 `score()` 和 `score_batch()` | `create_ml_scorer(路径)` 加载模型 |
| [cue_pattern.py](file:///d:/何振鹏/可视化/ZHINENG/causal-graph-system/src/relation/cue_pattern.py) | 57 个因果提示词表 + `find_cues()` 匹配 | 规则模式的核心 |
| [train_ml_scorer.py](file:///d:/何振鹏/可视化/ZHINENG/causal-graph-system/src/relation/train_ml_scorer.py) | 训练工具（BERT/sklearn 双后端） | 有新数据时重新训练 |
| [mine_hard_negatives.py](file:///d:/何振鹏/可视化/ZHINENG/causal-graph-system/src/relation/mine_hard_negatives.py) | 硬负例挖掘工具 | 解决域偏移问题 |
| [convert_external_datasets.py](file:///d:/何振鹏/可视化/ZHINENG/causal-graph-system/src/relation/convert_external_datasets.py) | 外部数据集格式转换 | 新增数据源时用 |
| [_calibrate.py](file:///d:/何振鹏/可视化/ZHINENG/causal-graph-system/src/relation/_calibrate.py) | 提示词/特征权重标定 | 实验工具 |

### 2.2 运行脚本（项目根目录）

| 文件 | 用途 | 运行命令 |
|------|------|---------|
| [run_bert.py](file:///d:/何振鹏/可视化/ZHINENG/causal-graph-system/run_bert.py) | BERT 模型推理（GPU 批量，~34秒） | `python run_bert.py` |
| [run_extract.py](file:///d:/何振鹏/可视化/ZHINENG/causal-graph-system/run_extract.py) | 规则/sklearn 推理 | `python run_extract.py` |
| [run_compare.py](file:///d:/何振鹏/可视化/ZHINENG/causal-graph-system/run_compare.py) | sklearn vs BERT 对比 | `python run_compare.py` |
| [run_compare3.py](file:///d:/何振鹏/可视化/ZHINENG/causal-graph-system/run_compare3.py) | 三方对比（规则/CEC-BERT/合并-BERT） | `python run_compare3.py` |
| [run_threshold_sweep.py](file:///d:/何振鹏/可视化/ZHINENG/causal-graph-system/run_threshold_sweep.py) | 阈值扫描 | `python run_threshold_sweep.py` |

### 2.3 数据文件

#### 训练数据（`data/training/`）

| 文件 | 规模 | 说明 |
|------|------|------|
| `merged_causal.jsonl` | 28,319 条 | 合并数据集（CausalEventPairs + C-CSB + CEC） |
| `merged_causal_hn.jsonl` | 29,119 条 | 合并 + 800 条硬负例（用于最终模型训练） |
| `cec_hard_negatives.jsonl` | 800 条 | CEC 同文档内挖掘的硬负例 |

#### CEC 数据（`data/cec/`）

| 文件 | 规模 | 说明 |
|------|------|------|
| `cec_events.jsonl` | 1,407 个事件 | CEC 语料解析的事件（5 类突发事件 × 75 篇） |
| `cec_gold_relations.jsonl` | 207 条标注因果对 | 人工标注的金标准 |

#### 已训练模型（`data/models/`）

| 模型目录 | 类型 | 训练数据 | 推荐度 |
|---------|------|---------|--------|
| **`cec_causal_scorer_bert/`** | BERT（CEC-only） | 824 样本 | ⭐ **推荐**（F1 最高） |
| `merged_bert_hn/` | BERT（合并+硬负例） | 29,119 样本 | 召回率最高但精确率低 |
| `merged_bert/` | BERT（合并） | 28,319 样本 | 域偏移问题严重，不推荐单独用 |
| `merged_sklearn/` | sklearn（TF-IDF+LR） | 28,319 样本 | 无 GPU 时备选 |
| `cec_causal_scorer/` | sklearn（CEC-only） | 824 样本 | 早期基线 |

#### 输出示例（`data/output/`）

| 文件 | 说明 |
|------|------|
| `cec_bert_relations.jsonl` | BERT 模型在 CEC 上的输出（672 条） |
| `cec_relations.jsonl` | 规则模式输出 |
| `cec_relations_sklearn.jsonl` | sklearn 模型输出 |

### 2.4 测试（`tests/`）

| 文件 | 测试数 | 说明 |
|------|--------|------|
| [test_relation.py](file:///d:/何振鹏/可视化/ZHINENG/causal-graph-system/tests/test_relation.py) | 21 个 | 覆盖规则抽取、ML 打分、批量模式、硬负例等 |
| test_schemas.py | — | 数据结构验证 |
| test_io.py | — | IO 工具 |
| test_config.py | — | 配置 |

运行测试：
```powershell
$env:PYTHONPATH = ".vendor"
python -m pytest tests/test_relation.py -v
```

### 2.5 文档（`docs/`）

| 文件 | 说明 |
|------|------|
| `module2_relation_changelog.md` | 模块 2 完整变更记录（v1-v5） |

---

## 三、实验结果汇总

### 3.1 模型对比（CEC 1407 事件 / 207 标注对）

| 模型 | 训练数据 | 产出数 | 命中 | 召回率 | 精确率 | F1 |
|------|---------|--------|------|--------|--------|-----|
| A. 规则基线 | 无 | 1,142 | 42 | 0.2029 | 0.0368 | 0.0623 |
| B. CEC-only BERT ⭐ | 824 样本 | 672 | 74 | 0.3575 | 0.1101 | **0.1684** |
| C. 合并 BERT | 28,319 样本 | 9,554 | 105 | 0.5072 | 0.0110 | 0.0215 |
| D. 合并+硬负例 BERT | 29,119 样本 | — | — | — | — | 0.0402* |

> *模型 D 的验证集 F1=0.4024，但在 CEC 上仍有域偏移问题。

### 3.2 阈值扫描（合并 BERT）

| 阈值 | 产出 | 命中 | 召回率 | 精确率 | F1 |
|------|------|------|--------|--------|-----|
| 0.60 | 9,554 | 105 | 0.5072 | 0.0110 | 0.0215 |
| 0.80 | 7,853 | 94 | 0.4541 | 0.0120 | 0.0233 |
| 0.95 | 4,096 | 81 | 0.3913 | 0.0198 | 0.0376 |

### 3.3 硬负例训练曲线

| Epoch | 训练 Loss | 验证 Loss | 说明 |
|-------|----------|----------|------|
| 1 | 0.558 | 0.443 | 初步收敛 |
| 2 | 0.358 | **0.402** | 最佳（early stopping 点） |
| 3 | 0.226 | 0.504 | 过拟合 |

### 3.4 GPU 加速效果

| 模式 | 耗时 | 说明 |
|------|------|------|
| CPU 逐条 | ~10 分钟 | BERT 前向传播串行 |
| GPU 逐条 | 3 分 52 秒 | RTX 4070，2.6 倍提升 |
| **GPU 批量** | **34 秒** | 64 条/批，6.8 倍提升 |

---

## 四、快速上手指南

### 4.1 环境要求

```
Python 3.14+
torch (CUDA 版，用于 GPU 推理)
transformers >= 5.0
accelerate >= 1.1
scikit-learn (sklearn 后端备选)
```

### 4.2 最简调用（推荐方式）

```python
from src.common.io_utils import load_events
from src.relation.causal_relation import extract_relations
from src.relation.ml_scorer import create_ml_scorer

# 1. 加载事件
events = load_events("data/cec/cec_events.jsonl")

# 2. 加载 BERT 模型（推荐 CEC-only）
scorer = create_ml_scorer("data/models/cec_causal_scorer_bert")

# 3. 抽取因果关系
relations = extract_relations(events, ml_scorer=scorer)

# 4. 查看结果
for r in relations[:5]:
    print(f"{r.cause_event_id} -> {r.effect_event_id} | {r.relation_type} | conf={r.confidence:.3f}")
```

### 4.3 事件数据格式

```json
{
  "event_id": "CEC_001_e3",
  "doc_id": "CEC_001",
  "mention": "截至当日下午１４时，８日发生的地震",
  "time": "８日",
  "trigger": "地震",
  "char_offset": [0, 17],
  "arguments": []
}
```

### 4.4 输出格式

```json
{
  "relation_id": "R_001",
  "cause_event_id": "CEC_001_e3",
  "effect_event_id": "CEC_001_e4",
  "relation_type": "causal",
  "confidence": 0.83,
  "evidence": ["地震", "已经造成６人死亡"]
}
```

---

## 五、关键设计决策

| 决策 | 原因 |
|------|------|
| 规则模式保留 | 无 GPU 环境下的备选方案；规则可解释 |
| ML 模式遍历所有事件对 | 规则模式只处理有提示词的事件对，ML 模式通过邻近性过滤降低配对量 |
| CEC-only BERT 为推荐模型 | F1=0.1684 最高，合并数据集引入域偏移（短文本 vs 事件级文本） |
| 批量推理 64 条/批 | GPU 利用率与显存的平衡点 |
| 硬负例挖掘 | 教模型区分"词汇共现"和"因果关系"，缓解域偏移 |

---

## 六、已知问题与后续方向

| 问题 | 现状 | 后续方向 |
|------|------|---------|
| 域偏移 | 合并数据集在 CEC 上精确率低 | 两阶段微调（先合并预训练，再 CEC 微调） |
| CEC 召回率瓶颈 | 规则 20%，BERT 38% | 扩充事件级标注数据 |
| 部分产出仍不合理 | "受灾→死亡"等并列误判为因果 | 增加硬负例数量 + 对比学习 |
| BERT 训练过拟合 | Epoch 3 验证 loss 反弹 | 早停 or 数据增强 |

---

## 七、文件树总览

```
causal-graph-system/
├── src/relation/                    # 模块2 核心代码
│   ├── causal_relation.py            # 主入口 extract_relations()
│   ├── ml_scorer.py                  # ML 打分器（BERT/sklearn + 批量）
│   ├── cue_pattern.py                # 57 个因果提示词
│   ├── train_ml_scorer.py            # 训练工具
│   ├── mine_hard_negatives.py        # 硬负例挖掘
│   ├── convert_external_datasets.py  # 外部数据转换
│   └── _calibrate.py                # 权重标定
│
├── data/
│   ├── cec/                          # CEC 标注数据
│   │   ├── cec_events.jsonl          # 1407 事件
│   │   └── cec_gold_relations.jsonl  # 207 因果对
│   ├── training/                     # 训练数据集
│   │   ├── merged_causal.jsonl       # 28319 条
│   │   ├── merged_causal_hn.jsonl    # 29119 条（含硬负例）
│   │   └── cec_hard_negatives.jsonl  # 800 条硬负例
│   ├── models/                      # 已训练模型
│   │   ├── cec_causal_scorer_bert/   # ⭐ 推荐（CEC-only BERT）
│   │   ├── merged_bert_hn/           # 合并+硬负例 BERT
│   │   ├── merged_bert/              # 合并 BERT
│   │   ├── merged_sklearn/           # 合并 sklearn
│   │   └── cec_causal_scorer/        # CEC sklearn
│   └── output/                      # 推理输出
│       ├── cec_bert_relations.jsonl
│       └── ...
│
├── tests/
│   └── test_relation.py             # 21 个测试
│
├── docs/
│   └── module2_relation_changelog.md # 变更记录
│
├── run_bert.py                      # BERT 推理脚本
├── run_extract.py                  # 规则/sklearn 推理
├── run_compare.py                   # 模型对比
├── run_compare3.py                  # 三方对比
├── run_threshold_sweep.py           # 阈值扫描
└── 团队交接文档.md                   # 本文件
```
